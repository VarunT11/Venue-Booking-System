<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:layout_marginTop="16dp"
  android:layout_marginBottom="16dp">

  <ImageView
    android:id="@+id/imgItemProfileIcon"
    android:layout_width="14dp"
    android:layout_height="14dp"
    app:layout_constraintBottom_toBottomOf="@+id/tvItemProfileLabel"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="@+id/tvItemProfileLabel"
    app:srcCompat="@drawable/ic_logout" />

  <TextView
    android:id="@+id/tvItemProfileLabel"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="8dp"
    android:fontFamily="@font/poppins_regular"
    android:gravity="center_vertical|start"
    android:text="TextView"
    android:textColor="#0B0C24"
    android:textSize="14sp"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toStartOf="@+id/imgItemProfileArrow"
    app:layout_constraintStart_toEndOf="@+id/imgItemProfileIcon"
    app:layout_constraintTop_toTopOf="parent" />

  <ImageView
    android:id="@+id/imgItemProfileArrow"
    android:layout_width="12dp"
    android:layout_height="12dp"
    app:layout_constraintBottom_toBottomOf="@+id/tvItemProfileLabel"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toTopOf="@+id/tvItemProfileLabel"
    app:srcCompat="@drawable/ic_arrow_forward" />

</androidx.constraintlayout.widget.ConstraintLayout>